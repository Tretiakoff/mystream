{% extends 'MyStreamBundle::base.html.twig' %}

{% block title %}Modifier {{ video.title }}{% endblock %}

{% block body %}
    <h3>Modifier {{ video.title }}</h3>

    {{ form_start(form, {'name': 'edit', 'action': '/dashboard/' ~ video.id ~ '/edit'}) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}